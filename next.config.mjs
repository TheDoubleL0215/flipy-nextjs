/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com'], // Add here the domain for Google profile pictures
    },
}

export default nextConfig;
